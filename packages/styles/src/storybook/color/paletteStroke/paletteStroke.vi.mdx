import { Meta } from '@storybook/addon-docs/blocks'

<Meta title='@dxtmisha/vi/styles/color/paletteStroke - Nét vẽ bảng màu'/>

# `paletteStroke`

Mixin thiết lập màu `stroke` cho SVG bằng cách tạo chuỗi tìm kiếm qua các biến bảng màu đã chỉ định.

**Tham số:**
- `$palette: List | String` — danh sách tên biến bảng màu để tìm kiếm.
- `$color: Color | String` — màu dự phòng nếu không có biến bảng màu nào được xác định.
- `$opacity: Number | null` [null] — mức độ mờ tùy chọn [0..1].
- `$important: Boolean` [false] — nếu `true`, thêm `!important`.

## Ví dụ

```scss
@use '@dxtmisha/styles';

.icon-stroke-themed {
  @include styles.paletteStroke(('theme-primary', 'sys-primary'), #0000ff);
}
```
