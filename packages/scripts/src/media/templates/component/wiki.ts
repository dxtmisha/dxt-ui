// :component.once import { WikiStorybook } from '@dxt-ui/functional'
// :component.once import { wiki } from '@dxt-ui/media/wiki'

// :component.once import { wiki[Design] } from '../../../wiki/wiki.ts'

// :component.once const propsNames: string[] = [
// :propsNames [!] System label / Системная метка
// :propsNames [!] System label / Системная метка
// :component.once ]

// :component.once export const ComponentWikiStorybook = new WikiStorybook(
// :component.once   'Component',
// :component.once   propsNames,
// :component.once   wiki[Design],
// :component.once   wiki
// :component.once )
