import { Meta } from '@storybook/addon-docs/blocks'

<Meta title='@dxtmisha/ru/scripts/4. Создание компонентов'/>

# Создание и разработка компонентов

В дизайн-системе **DXT UI** процесс создания новых компонентов полностью автоматизирован с помощью команды `dxt-component`. Разработчику больше не нужно вручную создавать папки, настраивать шаблонные файлы, прописывать экспорты и конфигурации Storybook.

Инструмент берет на себя рутину и за доли секунды разворачивает эталонную архитектуру для каждого нового компонента.

## Преимущества автоматизации

### 1. Эталонная структура без рутины (Zero Boilerplate)
Вместо ручного создания файлов со сложными связями и импортами, разработчик создает только одну пустую папку. Команда `dxt-component` автоматически генерирует полную структуру, исключая опечатки в BEM-классах, путях и именах интерфейсов.

### 2. Строгая типизация через `DefineSetupFnComponent`
Сгенерированная точка входа `index.ts` использует типизированную обертку Vue 3, которая обеспечивает идеальный IntelliSense и точную проверку пропсов, слотов и событий в шаблонах проектов.

### 3. Готовая интеграция со Storybook и ИИ
Каждый новый компонент сразу готов к запуску в Storybook и содержит специализированные промпты `prompt.md` для совместной разработки с AI-ассистентами.

## Пошаговое руководство (Что нужно делать)

### Шаг 1. Создайте пустую папку компонента
В директории `src/components/` создайте новую пустую папку с именем компонента в PascalCase (например, `Badge` или `UserAvatar`):

```bash
mkdir src/components/Badge
```

### Шаг 2. Запустите команду генерации
Выполните команду `dxt-component` в терминале:

```bash
npx dxt-component
```

Генератор автоматически найдет пустую папку и создаст в ней полный комплект файлов:

```text
Component create: Badge
path: Badge
end
```

### Шаг 3. Изучите сгенерированную структуру
В папке компонента появится 8 готовых файлов:

```text
src/components/Badge/
├── Badge.vue                  # Шаблон Vue 3 SFC (<script setup>, типы, стили)
├── index.ts                   # Точка входа с типизацией DefineSetupFnComponent
├── types.ts                   # Интерфейсы Props, Emits, Slots и Defaults
├── materials/
│   └── prompt.md              # Промпт для ИИ по материалам компонента
├── subcomponents/
│   └── .gitignore             # Изоляция локальных подкомпонентов
└── wiki/
    ├── Badge.mdx              # Документация Storybook в формате MDX
    ├── Badge.stories.ts       # Интерактивные истории Storybook Vue 3
    └── prompt.md              # Промпт для ИИ по документированию в Wiki
```

### Шаг 4. Опишите свойства и типы компонента
Откройте `types.ts` и определите необходимые входные свойства (`Props`), события (`Emits`) и слоты (`Slots`):

```typescript
export type BadgeEmits = {
  click: [event: MouseEvent]
}

export interface BadgeSlots {
  default? (props: any): any
  icon? (props: any): any
}

export interface BadgeProps {
  label?: string
  color?: string
  outline?: boolean
}

export const BadgeDefaults: BadgeProps = {
  color: 'primary',
  outline: false
}
```

### Шаг 5. Реализуйте разметку и стили
Откройте `Badge.vue` и добавьте логику компонента и SCSS-стили:

```html
<script setup lang="ts">
import {
  BadgeDefaults,
  type BadgeEmits,
  type BadgeProps,
  type BadgeSlots
} from './types'

defineOptions({
  name: 'Badge'
})

withDefaults(defineProps<BadgeProps>(), BadgeDefaults)
defineEmits<BadgeEmits>()
defineSlots<BadgeSlots>()
</script>

<template>
  <div class="badge">
    <slot name="icon" />
    <slot>{{ label }}</slot>
  </div>
</template>

<style lang="scss">
.badge {
  display: inline-flex;
  align-items: center;
  gap: 4px;
}
</style>
```

### Шаг 6. Настройте документацию и превью в Storybook
- Для генерации историй и документации перетащите файл `wiki/prompt.md` в чат ИИ и попросите выполнить задачу (если кнопка отправки активна сразу после прикрепления файла, текст можно не писать).
- В файле `wiki/Badge.stories.ts` настройте тестовые сценарии использования компонента.
- В файле `wiki/Badge.mdx` добавьте текстовое описание, свойства и интерактивные демо.
- Запустите Storybook (`npm run storybook`) для проверки отображения компонента.

## Работа с директорией materials (Дизайн-материалы)

Папка `materials/` — это изолированное рабочее пространство компонента для сбора исходных дизайн-материалов, спецификаций и контекста разработки:

- **Файл `prompt.md`**: Системный файл инструкций для ИИ-ассистентов, автоматически связанный со стандартами дизайн-системы (`componentMaterials.md`). В него разработчик вносит ссылки на Figma, описание состояний, пропсов и любые требования к реализации — всё, что должен знать ИИ.
- **Дизайн-ассеты и скриншоты**: Сюда сохраняются снимки экранов макетов Figma, SVG-иконки, схемы и варианты оформления компонента.
- **Разработка через ИИ**: Перетащите файл `materials/prompt.md` в чат ИИ и попросите выполнить задачу (если в окне чата сразу активна кнопка отправки, текст можно не писать). ИИ создаст готовый код строго по требованиям.
- **Сохранение контекста**: Папка остаётся в репозитории как единый источник проектных решений, позволяя в будущем легко восстановить контекст создания компонента.

## Работа с директорией wiki (Документация и Storybook)

Папка `wiki/` — это пространство компонента, предназначенное для интерактивных историй Storybook и технической документации:

- **Файл `[ComponentName].mdx`**: Основная статья документации в формате MDX с интеграцией компонента `<StorybookMain />` из `@dxtmisha/wiki/storybook`, описанием пропсов и интерактивными демо.
- **Файл `[ComponentName].stories.ts`**: Исполняемые истории Storybook Vue 3, демонстрирующие различные состояния и сценарии использования компонента.
- **Файл `prompt.md`**: Системный файл инструкций для ИИ-ассистентов, автоматически связанный со стандартами документации (`componentWiki.md`). В него разработчик может вписать любые пожелания по описанию компонента, примеры или особенности использования.
- **Генерация через ИИ**: Перетащите файл `wiki/prompt.md` в чат ИИ и попросите выполнить задачу (если в окне чата сразу активна кнопка отправки, текст можно не писать). ИИ автоматически проанализирует код компонента, типы и сформирует готовые истории и документацию.

## Преимущества перед ручным созданием

- **Мгновенная скорость**: Создание всех 8 файлов занимает менее 1 секунды вместо 5–10 минут ручной рутины.
- **Полная архитектурная целостность**: Автоматически создаются все обязательные файлы, включая Storybook-истории, типизацию и промпты для ИИ.
- **100% точность кода**: Исключены человеческие ошибки, опечатки в BEM-классах, относительных путях и именах типов.
- **Готовность к разработке**: Компонент готов к открытию в Storybook сразу после выполнения команды.
