// md5:90408cb8df938d0bba978ae987a47e8e true
import type { ConstrClass } from '@dxtmisha/functional';

/** Connected component dependencies for table record. @keywords components, table */
export type TableRecordComponents = TableHeaderItemComponentInclude & TableItemComponentInclude;

/** Available events for table record. @keywords emits, events */
export type TableRecordEmits = TableHeaderItemEmits;

/** Exposed instance properties and methods for table record. @keywords expose */
export interface TableRecordExpose {}

/** Available slots for table record. @keywords slots */
export type TableRecordSlots = TableRecordItemSlots;

/** CSS class definitions for table record elements. @keywords classes, style */
export type TableRecordClasses = {
  /** Main element class name. @keywords main, class */
  main: ConstrClass;
};