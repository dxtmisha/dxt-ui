/**
 * Interface describing keyboard control properties.
 *
 * Интерфейс, описывающий свойства клавиатурного управления.
 */
export type ControlProps = {
  /** Whether keyboard control is enabled / Включено ли управление с клавиатуры */
  control?: boolean
}
