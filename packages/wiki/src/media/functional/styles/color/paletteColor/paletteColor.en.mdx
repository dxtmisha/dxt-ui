import { Meta } from '@storybook/addon-docs/blocks'

<Meta title='@dxtmisha/en/styles/color/paletteColor - Palette text color'/>

# `paletteColor`

The mixin sets the text `color` by generating a search chain across the specified palette variables.

**Parameters:**
- `$palette: List | String` — list of palette variable names to search.
- `$color: Color | String` — fallback color if no palette variables are defined.
- `$opacity: Number | null` [null] — optional opacity level [0..1].
- `$important: Boolean` [false] — if `true`, adds `!important`.

## Example

```scss
@use '@dxtmisha/styles';

.text-themed {
  @include styles.paletteColor(('theme-primary', 'sys-primary'), #0000ff);
}
```
