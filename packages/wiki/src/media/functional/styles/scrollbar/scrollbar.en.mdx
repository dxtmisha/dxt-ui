import { Meta } from '@storybook/addon-docs/blocks'

<Meta title='@dxtmisha/en/styles/scrollbar - Scrollbars'/>

# Scrollbars System

The library provides a mixin for managing element scrollbars appearance.

## Mixins

- `scrollbarHide()` — Hides the browser scrollbar visually while maintaining scroll functionality.
