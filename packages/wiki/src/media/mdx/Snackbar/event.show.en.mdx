### `show`

The event fires when a notification is displayed in the list.

**When triggered:**
- A new notification has been successfully added to the list
- The notification has become visible to the user
