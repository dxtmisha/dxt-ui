### `hide`

The event fires when a notification is hidden from the list.

**When triggered:**
- The notification display time has expired (automatic hiding)
- The user or program initiated the removal of the notification
- The notification list has been cleared

**Arguments:**
- `value`: `string` — Unique identifier of the notification
- `item`: `SnackbarValue` — Notification data object
