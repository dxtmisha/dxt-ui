### `back`

Transition to the previous slide.

**Type:** `() => void`

### `next`

Transition to the next slide.

**Type:** `() => void`

### `to`

Transition to the specified slide.

**Parameters:**
- `selected: string | number` — slide identifier

**Type:** `(selected: string | number) => void`

### `top`

Transition to the slide with animation from the top.

**Parameters:**
- `selected: string | number` — slide identifier

**Type:** `(selected: string | number) => void`

### `right`

Transition to the slide with animation from the right.

**Parameters:**
- `selected: string | number` — slide identifier

**Type:** `(selected: string | number) => void`

### `bottom`

Transition to the slide with animation from the bottom.

**Parameters:**
- `selected: string | number` — slide identifier

**Type:** `(selected: string | number) => void`

### `left`

Transition to the slide with animation from the left.

**Parameters:**
- `selected: string | number` — slide identifier

**Type:** `(selected: string | number) => void`

### `down`

Transition to the slide with animation downwards.

**Parameters:**
- `selected: string | number` — slide identifier

**Type:** `(selected: string | number) => void`

### `up`

Transition to the slide with animation upwards.

**Parameters:**
- `selected: string | number` — slide identifier

**Type:** `(selected: string | number) => void`
