### `window`

Event that triggers when the window state changes (open/close).

```ts
function onWindow (options: WindowEmitOptions) {
    // handle window state change
}
```

Parameters:
- `options` — `WindowEmitOptions` — window data: id, elements, and the `open` flag

