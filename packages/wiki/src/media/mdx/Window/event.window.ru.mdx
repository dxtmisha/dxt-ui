### `window`

Событие, которое срабатывает при изменении состояния окна (открытие/закрытие).

```ts
function onWindow (options: WindowEmitOptions) {
    // обработка изменения состояния окна
}
```

Параметры:
- `options` — `WindowEmitOptions` — данные окна: id, элементы и флаг `open`

