import { Source } from '@storybook/addon-docs/blocks';

### `headroomSticky`

Событие вызывается при изменении состояния прилипания (когда прокрутка пересекает ноль).

**Параметры:**
- `isSticky: boolean` — логический флаг активного состояния прилипания
