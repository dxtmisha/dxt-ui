### `scrollbarTop`

Event that triggers when the scroll position reaches or leaves the top boundary.

```ts
function onScrollbarTop (isTop: boolean) {
    // handle reaching/leaving top boundary event
}
```

Parameters:
- `isTop` - `boolean` - true if scroll is at the top position, false — if it left the top position

