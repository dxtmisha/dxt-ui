import { Source } from '@storybook/addon-docs/blocks';

### `headroomSticky`

Event triggered when sticky state changes (when scroll position crosses zero).

**Parameters:**
- `isSticky: boolean` — boolean indicating whether sticky positioning is active
