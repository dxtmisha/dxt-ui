### `scrollbarLeaveTop`

Событие, которое срабатывает при покидании верхней границы скролла.

```ts
function onScrollbarLeaveTop () {
    // обработка события покидания верхней границы
}
```

Срабатывает в момент начала прокрутки от самого верха контента.

