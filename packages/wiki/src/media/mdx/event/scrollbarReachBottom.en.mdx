### `scrollbarReachBottom`

Event that triggers when the scroll reaches the bottom boundary.

```ts
function onScrollbarReachBottom () {
    // handle reaching bottom boundary event
}
```

This event fires at the moment the very bottom of the content is reached.

