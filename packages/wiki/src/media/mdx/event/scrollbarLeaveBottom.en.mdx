### `scrollbarLeaveBottom`

Event that triggers when the scroll leaves the bottom boundary.

```ts
function onScrollbarLeaveBottom () {
    // handle leaving bottom boundary event
}
```

Fires at the moment scrolling starts from the very bottom upwards.

