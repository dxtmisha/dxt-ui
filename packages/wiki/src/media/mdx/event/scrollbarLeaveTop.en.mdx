### `scrollbarLeaveTop`

Event that triggers when the scroll leaves the top boundary.

```ts
function onScrollbarLeaveTop () {
    // handle leaving top boundary event
}
```

This event fires at the moment scrolling begins from the very top.

