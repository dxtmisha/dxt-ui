### `scrollbarReachBottom`

Срабатывает, когда скролл достигает нижней границы.

```ts
function onScrollbarReachBottom () {
    // обработка достижения нижней границы
}
```

Одноразовый момент в точке самого низа контента.

