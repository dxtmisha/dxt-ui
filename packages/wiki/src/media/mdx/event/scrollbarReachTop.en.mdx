### `scrollbarReachTop`

Event that triggers when the scroll reaches the top boundary.

```ts
function onScrollbarReachTop () {
    // handle reaching top boundary event
}
```

This event fires at the moment the very top of the content is reached.

