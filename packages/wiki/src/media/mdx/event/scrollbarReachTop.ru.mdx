### `scrollbarReachTop`

Срабатывает, когда скролл достигает верхней границы.

```ts
function onScrollbarReachTop () {
    // обработка достижения верхней границы
}
```

Это одноразовый момент в точке самого верха контента.

