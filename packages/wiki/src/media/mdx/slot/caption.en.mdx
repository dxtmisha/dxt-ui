### `caption`

Slot for passing caption content to the component. It's an alternative to the `caption` prop and allows passing elements instead of plain text.

This slot allows you to:
- Use an alternative to the `caption` property
- Pass elements instead of plain text
- Use HTML or components in the caption

The `caption` slot works as a replacement for the `caption` prop and allows passing any elements for more flexible caption content setup.
