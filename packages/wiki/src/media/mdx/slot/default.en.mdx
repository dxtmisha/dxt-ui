### `default`

Main slot for placing content inside the component.

This slot allows you to:
- Place any custom content inside the component
- Pass text, HTML elements, or other Vue components
- Customize the internal content of the component

The `default` slot is the primary way to add content to the component and supports all standard Vue slot capabilities.
