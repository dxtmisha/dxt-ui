### `title`

Slot for placing component title in the header.

**Parameters:**
- `props: ControlItem` — object with component data

**Returns:** `VNode` — title element

```html
<Component>
  <template #title>
    <h2>Title</h2>
  </template>
</Component>
```
