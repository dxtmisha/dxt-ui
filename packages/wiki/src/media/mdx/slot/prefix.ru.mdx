### `prefix`

Слот для размещения контента в начале компонента, перед основным содержимым.

**Возвращает:** `VNode` — элемент с классом `{className}__prefix` и атрибутом `data-event-type="prefix"`

```html
<Component>
  <template #prefix>
    <Icon name="arrow" />
  </template>
</Component>
```
