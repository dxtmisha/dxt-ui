### `prefix`

Slot for placing content at the beginning of the component, before the main content.

**Returns:** `VNode` — element with class `{className}__prefix` and attribute `data-event-type="prefix"`

```html
<Component>
  <template #prefix>
    <Icon name="arrow" />
  </template>
</Component>
```
