### `footer`

Слот для размещения содержимого в нижней части компонента.

**Параметры:**
- `props: ControlItem` — объект с данными компонента

**Возвращает:** `VNode` — элемент подвала

```html
<Component>
  <template #footer>
    <div>Дополнительная информация</div>
  </template>
</Component>
```
