### `label`

Слот для размещения пользовательского содержимого метки компонента вместо текстового значения.

**Возвращает:** `VNode` — элемент с классом `{className}__label`

```html
<Component>
  <template #label>
    <strong>Важная метка</strong>
  </template>
</Component>
```
