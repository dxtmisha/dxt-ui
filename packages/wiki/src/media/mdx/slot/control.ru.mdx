### `control`

Слот для размещения элементов управления компонентом (кнопки, переключатели и т.д.).

**Параметры:**
- `props: ControlItem` — объект с данными элемента управления

**Возвращает:** `VNode` — элементы управления компонентом
