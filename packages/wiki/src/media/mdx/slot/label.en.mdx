### `label`

Slot for passing label content to the component. It's an alternative to the `label` prop and allows passing elements instead of plain text.

This slot allows you to:
- Use an alternative to the `label` property
- Pass elements instead of plain text
- Use HTML or components in the label

The `label` slot works as a replacement for the `label` prop and allows passing any elements for more flexible label content setup.

