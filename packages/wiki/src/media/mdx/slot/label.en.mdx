### `label`

Slot for placing custom label content instead of text value.

**Returns:** `VNode` — element with class `{className}__label`

```html
<Component>
  <template #label>
    <strong>Important label</strong>
  </template>
</Component>
```
