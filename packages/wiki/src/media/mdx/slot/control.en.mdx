### `control`

Slot for rendering the component’s control (trigger) element.

This slot lets you:
- Place a button, icon, or any interactive trigger
- Receive control data via a scoped slot (if provided by the component)
- Customize open/close logic and extra actions

