### `title`

Слот для размещения заголовка компонента в шапке.

**Параметры:**
- `props: ControlItem` — объект с данными компонента

**Возвращает:** `VNode` — элемент заголовка

```html
<Component>
  <template #title>
    <h2>Заголовок</h2>
  </template>
</Component>
```
