### `footer`

Slot for placing content at the bottom of the component.

**Parameters:**
- `props: ControlItem` — object with component data

**Returns:** `VNode` — footer element

```html
<Component>
  <template #footer>
    <div>Additional information</div>
  </template>
</Component>
```
