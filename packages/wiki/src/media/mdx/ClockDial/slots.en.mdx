- `default?(props: any): any` — Default slot for custom content rendered at the center of the clock dial (e.g. digital time string or icon).
- `item?(props: ClockDialMarkSlot): any` — Slot for custom rendering of individual clock mark items.

> **ClockDialMarkSlot:**
> - `item: ClockDialMarkItem` — mark item configuration object.
