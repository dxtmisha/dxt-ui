### `head`

Slot for placing the group header content with toggle functionality.

- Contains the clickable header element that controls group expansion/collapse
- Receives `open` parameter indicating current group state
- Typically, contains group title, icon, and toggle controls
- Manages the visual state of the group header
