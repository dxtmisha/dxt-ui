- `isSticky: ComputedRef<boolean>` — Computed state indicating whether scroll position is greater than zero and sticky behavior is active.
- `update(): void` — Recalculates scroll metrics and updates headroom states and element transformation styles.
