### HTML Content

The Bullet component supports rendering direct HTML strings using the `html` property.

When HTML is provided, the component scans the string for standard `<li>` elements and automatically replaces/appends the system's list item class names to them. This ensures that any lists rendered from external APIs or rich text editors inherit the design system's exact appearance.
