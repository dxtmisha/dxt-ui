- `get(): CropAreaCoordinator` — Возвращает текущий массив координат `[сверху, справа, снизу, слева]` в процентах.
- `set(coordinator: CropAreaCoordinator): void` — Программно устанавливает новые значения координат и обновляет пользовательские CSS-переменные на DOM-элементе.
- `reset(): void` — Сбрасывает координаты к начальным значениям (из свойства `value` или `[0, 0, 0, 0]`) и пересчитывает стили компонента.

> **CropAreaCoordinator:**
> - `[number, number, number, number]` — кортеж из 4 чисел в процентах, задающих отступы `[сверху, справа, снизу, слева]`.
