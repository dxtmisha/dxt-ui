### `contextBottom`

Slot for the bottom contextual area of the Menu window.

This slot lets you:
- Place extra content below the list (hints, final actions)
- Show dynamic blocks depending on window/selection state
- Access control data (MenuControlItem) if needed

