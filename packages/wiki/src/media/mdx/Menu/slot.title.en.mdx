### `title`

Slot for the Menu window title.

This slot lets you:
- Display title, subtitle, status, or counters
- Dynamically change content based on state
- Access control data (MenuControlItem) if needed

