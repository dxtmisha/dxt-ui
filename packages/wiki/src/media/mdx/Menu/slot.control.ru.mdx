### `control`

Слот для рендеринга элемента управления (триггера) Menu.

Этот слот позволяет:
- Разместить кнопку, иконку или любой триггер открытия/закрытия
- Получать данные контрола через scoped‑slot (MenuControlItem)
- Использовать признаки выбора (isSelected, selectedNames, selectedValues)

