### `control`

Slot to render the Menu trigger/control.

This slot lets you:
- Place a button, icon, or any trigger for open/close
- Receive control data via scoped slot (MenuControlItem)
- Use selection state (isSelected, selectedNames, selectedValues)

