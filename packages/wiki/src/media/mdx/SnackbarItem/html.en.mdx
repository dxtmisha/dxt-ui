## HTML Content

The `html` property allows you to insert arbitrary HTML code directly into the body of the SnackbarItem.

> [!WARNING]
> Using the `html` property is not recommended. It can be unsafe and may lead to XSS vulnerabilities. Consider using the `component` property or slots for safer and more flexible rendering of complex content.
