- `getMotionTransformElement(): ConstrBind<MotionTransformExpose> | undefined` — Returns the nested MotionTransform component's expose API, providing programmatic access to control methods.

> **Methods via getMotionTransformElement():**
> - `getOpen(): boolean` — returns open state
> - `isShow(): boolean` — returns visibility state
> - `setOpen(open: boolean): void` — set open state
> - `toOpen(): void` — open component
> - `toClose(): void` — close component
> - `toggle(): void` — toggle state
