- `motionTransformElement: Ref<ConstrBind<MotionTransformExpose> | undefined>` — Reactive reference to the nested MotionTransform component's expose API, providing programmatic access to control methods.

> **Methods via motionTransformElement.value:**
> - `open: Ref<boolean>` — reactive open state
> - `isShow: ComputedRef<boolean>` — computed visibility state
> - `setOpen(open: boolean): void` — set open state
> - `toOpen(): void` — open component
> - `toClose(): void` — close component
> - `toggle(): void` — toggle state
