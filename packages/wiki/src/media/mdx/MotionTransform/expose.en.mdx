- `open: Ref<boolean>` — reactive open state.
- `isShow: ComputedRef<boolean>` — computed visibility state.
- `setOpen(open: boolean): void` — sets the open state.
- `toOpen(): void` — transitions to the open state.
- `toClose(): void` — transitions to the close state.
- `toggle(): void` — toggles the open state.
