- `value: Ref<string>` — Final formatted mask value.
- `valueBasic: Ref<string>` — Basic mask value without formatting (only entered characters).
- `set(value: string): boolean` — Sets new value and reinitializes the mask. Returns `true` if changed.
