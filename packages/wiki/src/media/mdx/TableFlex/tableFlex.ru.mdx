import { Source } from '@storybook/addon-docs/blocks';

Компонент флекс-таблицы, координирующий сеточные индексы колонок и список строк данных для визуализации табличной информации.

Компонент `TableFlex` обеспечивает рендеринг структурированных наборов данных на основе флекс-верстки. Он объединяет конфигурации колонок (`columns`), настройки шапки (`header`), массивы строк (`list`), липкую шапку (`headerTop`), зафиксированные левые колонки (`stickyLeft`) и форматирование ячеек.

**Ключевые особенности:**

- Структурированное представление наборов данных в виде флекс-таблицы
- Автоматическое извлечение данных на основе массива колонок `columns`
- Поддержка многоуровневых иерархических шапок с помощью свойства `header`
- Зафиксированная шапка (`headerTop`) и липкие левые столбцы (`stickyLeft`)
- Настройка выравнивания ячеек, выбора строк и атрибутов на уровне колонок (`tableItemColumnAttrs`)
- Кастомные слоты для каждого индекса столбца для гибкого оформления содержимого

<Source
    code={`
<script setup>
const columns = ['id', 'name', 'role', 'status']
const header = { id: 'ID', name: 'Имя', role: 'Должность', status: 'Статус' }
const list = [
  { id: '1', name: 'Алиса', role: 'Разработчик', status: 'Активен' },
  { id: '2', name: 'Боб', role: 'Дизайнер', status: 'В ожидании' }
]
</script>

<template>
  <TableFlex
    :columns="columns"
    :header="header"
    :list="list"
    :headerTop="true"
  />
</template>
  `}
    language="html"
/>

> TableFlex предоставляет полную оркестрацию верстки на основе flexbox для табличных данных с поддержкой фиксации шапки, липких столбцов и переопределения слотов.
