### `leaveBottom`

Событие, которое срабатывает при покидании нижней границы скролла.

```ts
function onLeaveBottom () {
    // обработка события покидания нижней границы
}
```

Это событие срабатывает только в момент начала прокрутки от самого низа контента вверх.
