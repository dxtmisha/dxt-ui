### `bottom`

Event that triggers when the scroll position reaches or leaves the bottom boundary.

```ts
function onBottom (isBottom: boolean) {
    // handle reaching/leaving bottom boundary event
}
```

Parameters:
- `isBottom` - `boolean` - true if scroll is at the bottom position, false if it left the bottom position
