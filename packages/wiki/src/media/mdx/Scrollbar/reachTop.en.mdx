### `reachTop`

Event that triggers when the scroll reaches the top boundary.

```ts
function onReachTop () {
    // handle reaching top boundary event
}
```

This event only fires at the moment when the very top of the content is reached.
