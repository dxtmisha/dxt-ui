### `reachTop`

Событие, которое срабатывает при достижении верхней границы скролла.

```ts
function onReachTop () {
    // обработка события достижения верхней границы
}
```

Это событие срабатывает только в момент достижения самого верха контента.
