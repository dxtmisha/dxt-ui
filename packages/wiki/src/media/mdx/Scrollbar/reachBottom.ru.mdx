### `reachBottom`

Событие, которое срабатывает при достижении нижней границы скролла.

```ts
function onReachBottom () {
    // обработка события достижения нижней границы
}
```

Это событие срабатывает только в момент достижения самого низа контента.
