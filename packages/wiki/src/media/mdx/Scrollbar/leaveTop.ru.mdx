### `leaveTop`

Событие, которое срабатывает при покидании верхней границы скролла.

```ts
function onLeaveTop () {
    // обработка события покидания верхней границы
}
```

Это событие срабатывает только в момент начала прокрутки от самого верха контента.
