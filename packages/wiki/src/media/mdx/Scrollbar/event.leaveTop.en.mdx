### `leaveTop`

Event that triggers when leaving the top boundary of the scroll.

```ts
function onLeaveTop () {
    // handle leaving top boundary event
}
```

This event only fires at the moment when scrolling starts from the very top of the content.
