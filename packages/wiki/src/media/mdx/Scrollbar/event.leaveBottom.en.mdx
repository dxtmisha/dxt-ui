### `leaveBottom`

Event that triggers when leaving the bottom boundary of the scroll.

```ts
function onLeaveBottom () {
    // handle leaving bottom boundary event
}
```

This event only fires at the moment when scrolling starts upward from the very bottom of the content.
