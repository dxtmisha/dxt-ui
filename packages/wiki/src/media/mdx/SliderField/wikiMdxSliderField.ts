import type { StorybookComponentsMdxItem } from '../../../types/storybookTypes'

import sliderFieldEn from './sliderField.en.mdx'
import sliderFieldRu from './sliderField.ru.mdx'

export const wikiMdxSliderField: StorybookComponentsMdxItem = {
  name: 'SliderField',
  descriptions: {
    sliderField: {
      en: sliderFieldEn,
      ru: sliderFieldRu
    }
  }
}
