import type { StorybookComponentsMdxItem } from '../../../types/storybookTypes'

import rippleEn from './ripple.en.mdx'
import rippleRu from './ripple.ru.mdx'

export const wikiMdxRipple: StorybookComponentsMdxItem = {
  name: 'Ripple',
  descriptions: {
    ripple: {
      en: rippleEn,
      ru: rippleRu
    }
  }
}
