### `default`

Main input slot for Field – place the actual interactive control here.

- Receives props: `{ id, className, classHidden }`
- Always bind both `:id="id"` and `:class="className"` to keep accessibility + styling
- `classHidden` can be used for a hidden mirror / measurement input if needed
- Avoid extra wrapper elements to prevent layout / sizing breaks
- Suitable for `<input>`, `<textarea>`, masked or custom input components
