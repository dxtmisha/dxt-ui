import { wikiSlider } from './wikiSlider'

import { type StorybookArgsToList } from '../../types/storybookTypes'

/**
 * The wikiSliderField object contains descriptions of properties for the SliderField component
 *
 * Объект wikiSliderField содержит описание свойств для компонента SliderField
 */
export const wikiSliderField: StorybookArgsToList = {
  'sliderField.marks': wikiSlider['slider.marks']
}
