# Coding Guidelines

- Type files must have the `Types` suffix in their name (e.g. `*Types.ts` or `*Types.d.ts`).
- Within class method subgroups, group elements semantically by logical pairs/rules (e.g., `min` / `max`, `width` / `height`, `x` / `y` / `z`) before falling back to alphabetical sorting.

